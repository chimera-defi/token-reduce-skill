#!/usr/bin/env node

process.stdout.write(
  [
    "token-reduce is active.",
    "Start ambiguous repo discovery with ./scripts/token-reduce-paths.sh topic words.",
    "MCP tools: token_reduce_paths · token_reduce_snippet · token_reduce_benchmark · token_reduce_install_info",
  ].join("\n") + "\n"
);
