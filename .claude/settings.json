{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "timeout 20 uv run \"${CLAUDE_PROJECT_DIR:-.}/scripts/remind-token-reduce.py\" || exit 0"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "T=\"${CLAUDE_PROJECT_DIR:-.}/scripts/enforce-token-reduce-first.py\"; timeout 20 uv run \"$T\"; ec=$?; if [ \"$ec\" -eq 2 ] && [ -f \"$T\" ]; then exit 2; fi; exit 0"
          },
          {
            "type": "command",
            "command": "[ -x ~/.claude/hooks/rtk-rewrite.sh ] && ~/.claude/hooks/rtk-rewrite.sh || exit 0"
          }
        ]
      },
      {
        "matcher": "Glob",
        "hooks": [
          {
            "type": "command",
            "command": "T=\"${CLAUDE_PROJECT_DIR:-.}/scripts/enforce-token-reduce-first.py\"; timeout 20 uv run \"$T\"; ec=$?; if [ \"$ec\" -eq 2 ] && [ -f \"$T\" ]; then exit 2; fi; exit 0"
          }
        ]
      },
      {
        "matcher": "Grep",
        "hooks": [
          {
            "type": "command",
            "command": "T=\"${CLAUDE_PROJECT_DIR:-.}/scripts/enforce-token-reduce-first.py\"; timeout 20 uv run \"$T\"; ec=$?; if [ \"$ec\" -eq 2 ] && [ -f \"$T\" ]; then exit 2; fi; exit 0"
          }
        ]
      },
      {
        "matcher": "Read",
        "hooks": [
          {
            "type": "command",
            "command": "T=\"${CLAUDE_PROJECT_DIR:-.}/scripts/enforce-token-reduce-first.py\"; timeout 20 uv run \"$T\"; ec=$?; if [ \"$ec\" -eq 2 ] && [ -f \"$T\" ]; then exit 2; fi; exit 0"
          }
        ]
      }
    ]
  }
}
