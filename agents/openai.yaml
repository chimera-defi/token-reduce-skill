interface:
  display_name: "Token Reduce"
  short_description: "Find the smallest useful repo context."
  default_prompt: "Use $token-reduce to find the smallest useful repo context before reading files."
policy:
  allow_implicit_invocation: true
